{
  "name": "obsidian-personal-finance-dashboard",
  "version": "1.0.0",
  "description": "A personal finance dashboard plugin for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production && npm run copy-dist",
    "copy-dist": "rm -rf dist && mkdir -p dist && cp main.js manifest.json styles.css dist/",
    "setup-fixture": "bash scripts/setup-fixture.sh"
  },
  "keywords": [
    "obsidian",
    "finance",
    "dashboard"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "0.17.3",
    "obsidian": "latest",
    "tslib": "2.6.2",
    "typescript": "5.3.3"
  },
  "dependencies": {
    "chart.js": "^4.4.1"
  },
  "volta": {
    "node": "20.20.2"
  }
}
